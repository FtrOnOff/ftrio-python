"""Runnable demonstration of FtrIO toggles in action.

Run with ``python -m playground`` from the repository root. Mirrors the .NET
PlaygroundConsole: it cycles four users every two seconds and prints each
toggle's ON/OFF state, honouring live edits to ``appsettings.json``.
"""
